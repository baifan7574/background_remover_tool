# 📦 上传文件清单

**用途**: 部署到 PythonAnywhere 时需要上传的文件

---

## ⭐ 必须上传的文件

### 1. 核心应用文件
- [x] `sk_app.py` - 主应用文件（最重要）
- [x] `data_manager.py` - 数据管理器
- [x] `requirements.txt` - 依赖包列表

### 2. 前端文件目录
- [x] `frontend/index.html` - 首页
- [x] `frontend/payment.html` - 支付页面
- [x] `frontend/admin_login.html` - 后台登录页
- [x] `frontend/admin_orders.html` - 订单管理页
- [x] `frontend/payment.js` - 支付脚本
- [x] `frontend/js/app.js` - 主应用脚本
- [x] `frontend/js/auth.js` - 认证脚本
- [x] `frontend/css/style.css` - 样式文件
- [x] `frontend/images/alipay_qrcode.png` - 支付宝收款码 ⚠️
- [x] `frontend/images/wechat_qrcode.png` - 微信收款码 ⚠️

---

## 📁 目录结构（上传后应该是这样）

```
/home/baifan7574/background_remover_tool/
├── sk_app.py
├── data_manager.py
├── requirements.txt
├── frontend/
│   ├── index.html
│   ├── payment.html
│   ├── admin_login.html
│   ├── admin_orders.html
│   ├── payment.js
│   ├── js/
│   │   ├── app.js
│   │   └── auth.js
│   ├── css/
│   │   └── style.css
│   └── images/
│       ├── alipay_qrcode.png
│       └── wechat_qrcode.png
└── data/              # 会自动创建，也可以上传空目录
    ├── users.json
    ├── profiles.json
    ├── usage.json
    ├── invites.json
    └── orders.json
```

---

## ⚠️ 重要提醒

1. **收款码图片**: 确保已上传您的支付宝和微信收款码
2. **数据目录**: `data/` 目录会自动创建，但可以上传空目录
3. **文件权限**: 上传后确保文件有读取权限

---

## 🚀 快速上传方法

### 方法1: 使用 Files 页面上传
1. 登录 PythonAnywhere
2. 点击 "Files"
3. 进入 `/home/baifan7574/`
4. 创建 `background_remover_tool` 目录
5. 逐个上传文件

### 方法2: 打包上传（推荐）
1. 在本地将文件打包成 ZIP
2. 上传 ZIP 文件
3. 在 Console 中解压：
   ```bash
   cd ~/background_remover_tool
   unzip 上传的文件.zip
   ```

---

**文件清单完成**: 2025-11-25






